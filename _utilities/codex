#!/bin/bash
set -e

# set OpenAI/Codex profile to use
MY_PROFILE="work"                # john@scalara.com
# MY_PROFILE="personal"            # johnstanforth@gmail.com

# set Codex model to use
CODEX_MODEL="gpt-5.5"

# set Codex reasoning-level
# CODEX_REASONING="xhigh"
CODEX_REASONING="high"



# if [ "$1" = "--research" ]; then
    # export CLAUDE_STREAM_IDLE_TIMEOUT_MS=1200000     # 20 minutes
    # export CLAUDE_ENABLE_STREAM_WATCHDOG=1
    # shift  # pop --research so the real claude doesn't see it
    # echo "[claude-wrapper] --research mode enabled: stream idle timeout = 20 minutes"
# else
    # export CLAUDE_STREAM_IDLE_TIMEOUT_MS=420000      # 7 minutes
    # export CLAUDE_ENABLE_STREAM_WATCHDOG=1
    # echo "[claude-wrapper] normal mode: stream idle timeout = 7 minutes (pass --research as first arg for 20-minute research mode)"
# fi

CLI_BIN="$HOME/.npm-global/bin/codex"

echo "[codex-wrapper] Launching with profile: ${MY_PROFILE} ... [${CODEX_MODEL} : ${CODEX_REASONING}]"
export CODEX_HOME="${HOME}/.codex-${MY_PROFILE}"
exec "$CLI_BIN" --model ${CODEX_MODEL} -c model_reasoning_effort=${CODEX_REASONING} "$@"

