# _canonical/backlog — backlog for the canonical project-creation system

Tracked issues specific to the canonical template + New Project workflow (`_canonical/`). Kept here deliberately — rather than in `../_backlog_TODOs/` — because the rest of MetaProject's directory structure is in flux, and these canonical-system items are high-priority enough to want a stable home right next to what they concern.

Format: one `CANON-NNN-slug.md` per item, mirroring the `LATER-NNN` style in `../_backlog_TODOs/` (a Captured / Status / Related header, then the body). The `CANON-` numbering is independent of the global `LATER-` sequence.

| ID | Title | Status |
|----|-------|--------|
| CANON-001 | Canonicalize `run_dev_server.sh` (skeleton-gen for new projects + fleet standardization) | OPEN — gated by CANON-002 |
| CANON-002 | Finalize & standardize the project-template directory structure | OPEN |
