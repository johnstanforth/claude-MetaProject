# LATER-011 — Decide what to do with the `aixodev-openhands` repo (figure out what it even is / whether anything is salvageable)

- **Captured:** 2026-06-28 (John + Claude), while populating the GEN2 DB with the on-disk `~/Code` repo inventory.
- **Status:** open — needs a decision before modeling.
- **Owning project:** AIXO.Dev Platform.

## The ask

`aixodev-openhands` (`~/Code/AIXO.Dev/aixodev-openhands`) was on disk but **deliberately NOT added** to the GEN2 model alongside the rest of the AIXO.Dev family. Unlike the "finalize naming" batch ([LATER-012](LATER-012-finalize-naming-for-newly-modeled-repos.md) — those are real repos that just need name/PL decisions), this one is a **"figure out what this repo even is, and whether there's anything here worth salvaging"** question — most likely an early **OpenHands** (the open-source AI software-dev agent) integration experiment. Decide: salvage + model it (as what — a Build-Line under AIXO.Dev Platform? an Idea?), keep-but-park it, or archive/discard. Resolve before it gets pulled into any modeling or migration pass.
